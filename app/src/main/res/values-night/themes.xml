<resources xmlns:tools="http://schemas.android.com/tools">


    <!-- Expanded Title Style -->
    <style name="CollapsingToolbarExpanded" parent="TextAppearance.AppCompat.Large">
        <item name="android:textSize">32sp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Collapsed Title Style -->
    <style name="CollapsingToolbarCollapsed" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.ACCPause" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/material_dynamic_neutral_variant20</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>"
    </style>
</resources>